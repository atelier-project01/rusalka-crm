/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Hide the floating dev-mode indicator so it doesn't overlap the nav footer.
  devIndicators: false,
};

export default nextConfig;
