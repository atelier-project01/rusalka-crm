# ecommerce Supabase (eCommerce-prod project) — owns customers, orders, quiz,
# the storefront catalog (skincare_products) and the new CRM tables.
# Development points at the TEST branch (uieuy…); production at the main branch
# (kdjcb…). Copy to .env.local and fill in. Never commit .env.local.

# Dev / test branch:
NEXT_PUBLIC_SUPABASE_URL=https://uieuyprfexopmxgivssl.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Production (main branch) — set in the deploy environment, not here:
# NEXT_PUBLIC_SUPABASE_URL=https://kdjcbxjagaltvynvshkj.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# NOTE: GTIN / fulfillment-studio data still lives in the ERP project
# (recip3-prod: test ztqqm / prod sxrvr). The embedded Barcode/Fulfillment
# modules read from there — cross-project — until/if consolidated.
